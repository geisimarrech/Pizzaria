<h1>Listing order_flavors</h1>

<table>
  <tr>
    <th>Flavor</th>
    <th>Order product</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @order_flavors.each do |order_flavor| %>
  <tr>
    <td><%= order_flavor.flavor_id %></td>
    <td><%= order_flavor.order_product_id %></td>
    <td><%= link_to 'Show', order_flavor %></td>
    <td><%= link_to 'Edit', edit_order_flavor_path(order_flavor) %></td>
    <td><%= link_to 'Destroy', order_flavor, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Order flavor', new_order_flavor_path %>
